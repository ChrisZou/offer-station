# 求职工作台产品决策记录

## 文档用途

本文件记录已经确认且会影响产品、UI 或技术实现的决策。若早期效果图、旧文档或 Demo 文案与本文件冲突，以本文件及其关联的最新版需求文档为准。

| 项目 | 内容 |
| --- | --- |
| 当前版本 | V1.0 |
| 对应 PRD | [产品需求文档 V0.5](./产品需求文档.md) |
| 对应 UI | [UI 设计文档 V0.6](./UI设计文档.md) |
| 对应插件设计 | [浏览器插件功能与技术设计文档 V0.5](./浏览器插件功能与技术设计文档.md) |
| 更新日期 | 2026-08-18 |

## 已确认决策

### 产品与工作台

1. Web 工作台采用公开站点部署，不做成组织或工作区私有站点；公开访问不改变个人数据按账号隔离的要求。
2. 总览页按照最新总日程版效果图实现，聚合全部岗位的近期进度、待办和下一步，不默认指定单个重点岗位。
3. 简历管理入口首先进入简历列表。用户可预览已有简历，再进入“创建简历”，并根据不同岗位生成和保留多个岗位专属版本。
4. 简历创建页与编辑页是两个独立阶段：创建页负责选择岗位和职业档案素材，编辑页负责内容编辑、事实溯源、预览与导出。
5. 职业档案增加独立“个人信息”栏，允许按需填写姓名、电话、邮箱及其他求职常用字段；敏感字段默认不进入简历。
6. 全平台品牌位置统一使用第二版简洁 Logo；功能图标采用 SF Symbols 的语义和视觉语言，并在非 Apple 平台使用可合法分发的同风格替代。
7. 岗位库“收藏详情”改为 1100—1180px 级的大型分析窗口，展示 AI 匹配度、岗位总结、匹配/缺口能力、工作内容、岗位要求和折叠原始 JD，不再以原始 JD 作为唯一主体。

### 插件承载与浮窗

1. 插件默认打开方式由“浏览器侧边栏”改为“招聘页面内浮窗”；侧边栏保留为设置中的备选方式。
2. 默认浮窗尺寸最终确定为 420 × 620px；此前提出的 240 × 620px 已废弃。
3. 浮窗提供“收起”和“关闭”：收起保留横向会议软件风格悬浮栏，关闭完全隐藏；点击插件图标可以重新打开。
4. 收起横栏、展开浮窗都可拖动并共享同一位置。顶部 `•••` 把手始终水平居中，展开/收起时保持屏幕锚点不变。
5. 浮窗可以大部分拖出画面，为招聘页面留出空间，但 `•••` 可拖动部分必须完整保留在视口内。
6. 悬停、第一次鼠标移入、页面加载和岗位切换均不自动展开；用户必须点击横栏才能展开。
7. 浮窗可通过右下角调整大小。调整过程按动画帧更新外框并暂缓 iframe 内容重排，松手后恢复，避免明显卡顿。
8. 收起、关闭和岗位信息图标使用紧凑的 macOS/SF Symbols 风格，避免大圆形按钮和视觉过重的描边。
9. 收起横栏只展示第二版 Logo 和必要状态，不再显示冗余产品文字。

### 岗位识别与卡片

1. 公司名称和 Logo 必须从当前职位的同一公司信息卡成对提取。
2. 地图、地点缩略图、招聘者头像、学校图标和推荐职位图片不得作为当前公司 Logo；不可靠时使用通用职位图标。
3. 岗位卡薪资胶囊与岗位名称顶线对齐，价格文字不换行并保留足够内边距；长岗位名称使用省略号。
4. 浮窗不展示完整 JD 或完整岗位简介，改为短篇 AI 岗位总结。

### 匹配 Demo

1. “分析匹配”是插件主要价值点，但默认呈灰色待分析状态，不在识别岗位后自动运行。
2. 匹配只能由用户点击触发；收藏与匹配继续保持独立，收藏不自动分析，分析不自动收藏。
3. Demo 第一版暂不进行真实匹配识别，点击后展示明确标记为 Demo 的模拟结果。
4. 结果重点展示：匹配度、AI 岗位总结、已有/匹配技能、不具备/需要改进技能及简短投递建议。
5. 正式匹配时由 AI 提取条件与证据，最终分数由服务端固定规则计算：硬性条件 30%、核心技能 35%、相关项目与经历 25%、偏好及加分项 10%。未知信息不能算作不匹配。

### AI 技术选型

1. 首版统一使用 DeepSeek 官方模型 `deepseek-v4-flash`。
2. 官方 Base URL 为 `https://api.deepseek.com`；API Key 仅放在服务端，插件和 Web 前端不得持有。
3. 岗位提取、岗位总结、技能标签和普通文案使用非思考模式；匹配分析、技能差距和简历针对性改写使用思考模式。
4. 所有业务结果使用 JSON，并由服务端进行 Schema 校验。空内容、截断或格式失败自动重试一次，再失败则返回可恢复错误。
5. `dsv4plus` 是曾经误写的名称，不得进入配置；`deepseek-chat` 和 `deepseek-reasoner` 是旧模型标识，也不再使用。
6. Demo 阶段的固定匹配数据应与正式 API 使用相同 Schema，便于无 UI 改动替换为真实服务。

## 被替代的旧决策

| 旧决策 | 当前决策 |
| --- | --- |
| 默认浏览器侧边栏 | 默认页面浮窗，侧边栏备选 |
| 点击插件图标后才临时注入浮窗 | 支持页面常驻宿主；关闭后由图标重新打开 |
| 收起后竖向小栏或文字栏 | 横向会议软件风格悬浮栏，只保留必要元素 |
| 悬停或岗位切换自动展开 | 必须点击横栏才展开 |
| 收起栏与浮窗各自保存位置 | 两者共享位置并以 `•••` 为固定锚点 |
| 浮窗不能移出画面 | 可大部分移出，但拖动把手必须留在画面内 |
| 默认尺寸 240 × 620 | 默认尺寸 420 × 620 |
| 浮窗展示完整岗位描述/简介 | 展示 AI 岗位总结 |
| 匹配自动出现或默认彩色 | 默认灰色，用户点击后展示 Demo |
| 只展示主要匹配点和一个缺口 | 展示已有技能与需要改进技能两组信息 |
| 简历管理直接进入创建/编辑 | 先进入简历列表，再创建、编辑或预览 |
| AI 模型待定 / DSV4 Plus | `deepseek-v4-flash` |

## 尚未确认

1. 正式匹配 API 的响应时间目标和超时策略。
2. 免费额度、付费方案和单用户调用限额。
3. Side Panel 备选能力支持的最低 Chrome 版本。
4. 第一版是否将 Edge 作为正式支持平台，还是只做兼容性验证。
